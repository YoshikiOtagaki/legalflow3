## Get Case Request Mapping Template
## VTL template for getCase query

#set($id = $context.arguments.id)
#set($identity = $util.toJson($context.identity))

{
  "version": "2017-02-28",
  "operation": "Invoke",
  "payload": {
    "arguments": {
      "id": "$id"
    },
    "identity": $identity,
    "source": $util.toJson($context.source)
  }
}

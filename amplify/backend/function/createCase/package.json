{
  "name": "create-case-function",
  "version": "1.0.0",
  "description": "LegalFlow3 - Create Case Lambda Function",
  "main": "src/index.js",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf dist",
    "compile": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:memory:ci": "jest --testPathPattern=memory --ci",
    "lint": "eslint src/**/*.js",
    "lint:fix": "eslint src/**/*.js --fix",
    "lint:security": "eslint src/**/*.js --config .eslintrc.security.js",
    "deploy": "chmod +x deploy.sh && ./deploy.sh",
    "rollback": "chmod +x rollback.sh && ./rollback.sh",
    "monitor": "chmod +x monitor.sh && ./monitor.sh"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.450.0",
    "@aws-sdk/lib-dynamodb": "^3.450.0",
    "@aws-sdk/client-secrets-manager": "^3.450.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/jest": "^29.5.8",
    "@types/node": "^20.9.0",
    "@types/uuid": "^9.0.7",
    "eslint": "^8.53.0",
    "jest": "^29.7.0",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "aws",
    "lambda",
    "dynamodb",
    "legal",
    "case-management"
  ],
  "author": "LegalFlow3 Team",
  "license": "MIT"
}
